# Application Environment
# Options: development, production
ENVIRONMENT=development
DEBUG=true

# NOTE: API Keys and Redis URL are configured in src/config/config.json
# You can override config path here if needed:
# CONFIG_PATH=src/config/config.json

# Configuration File Path
# Defaults to src/config/config.json if not set
CONFIG_PATH=src/config/config.json
